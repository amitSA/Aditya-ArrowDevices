#include <fstream>
#include <string>
#include <iomanip>

using namespace std;


/*
This file can generate a sample problem for us.
The "probleminfo.txt" lets the user modify the problem spec 
    - ie. increase the number of data values for the test set
*/

/*
The target array always starts at 0, but can end at a user defined number
*/
int main()
{
	ifstream in();
	ostream out();

	int numTests;
	int targetSize;
	int minEntriesPerTarget = 1;
	int maxEntriesPerTarget;
	int percentRequirement;      // -1 if there is no percentRequirement

	













}